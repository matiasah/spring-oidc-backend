library identifier: 'maven-build-pipeline@master', retriever: modernSCM(
  [$class: 'GitSCMSource',
   remote: 'https://github.com/matiasah/maven-build-pipeline'])

mavenBuildPipeline(
  currentDirectory: "backend"
)
